name: 'kubectl-deploy'
description: 'Deploy to k8s with GitHub Action'
inputs:
  restart:
    description: 'The deployment to restart'
    required: true
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.restart }}
